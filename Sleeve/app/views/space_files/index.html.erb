<% @space_files.each do |spacefile| %>
    <p>
        <%= spacefile.name %>
        <br/>
        <%= spacefile.Lat %>
        <br/>
        <%= spacefile.Long %>
        <br/>
        <%= link_to "Download file", "files\\download\\#{spacefile.id}" %>
    </p>
<% end %>